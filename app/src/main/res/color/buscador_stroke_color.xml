<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cuando el buscador tiene el foco (clic para escribir) -->
    <item android:color="@color/emerald_500" android:state_focused="true" />

    <!-- Cuando el buscador NO tiene el foco -->
    <item android:color="@color/gray_400" />
</selector>